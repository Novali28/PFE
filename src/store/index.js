import Vue from 'vue'
import Vuex from 'vuex'
import {elements} from '@/model'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    elements: elements,
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
